# 001 · voicemode MVP — Plan

**Review:** ждёт владельца.

Ветка `main`. Каждый этап — серия атомарных коммитов, в конце этапа приложение собирается и то,
что сделано, можно потрогать. Оценки — рабочее время, грубо.

## До старта

- `brew install xcodegen`.
- `xcodebuild -downloadComponent MetalToolchain` — нужен MLX на этапе 6.
- Владелец наговаривает 25 фраз из списка ниже (этап 2 даёт для этого простой рекордер).

## Этапы

| # | Этап | Что внутри | Как проверяем | Оценка |
|---|---|---|---|---|
| 0 | Дизайн-макеты | Первый запуск (8 шагов), пилюля и остров во всех состояниях, меню, окно настроек, история. Холст с макетами на согласование | Владелец согласовал | 1 д |
| 1 | Каркас | `project.yml`, таргет приложения, `LSUIElement`, `MenuBarExtra`, хранилище настроек, сервис разрешений, подпись Apple Development, модули-заглушки | `xcodebuild` собирает, иконка в меню, статус разрешений виден | 0,5 д |
| 2 | Замеры | CLI-таргет `Bench`: рекордер фраз; прогон WhisperKit turbo / large-v3 и Apple DictationTranscriber; RUPunct; Qwen3.5 0.8B / 2B. Таблица: точность терминов, WER, пунктуация, задержка, память | Таблица в `implementation.md`, выбор моделей по умолчанию и порога LLM | 1 д |
| 3 | Ядро диктовки | Fn через event tap (удержание, двойное нажатие, Esc), `AudioCapture`, `WhisperKitEngine` + LiveAgreement, фильтр галлюцинаций, `ModelManager` (загрузка, SHA-256, подготовка), `DictationSession` | Зажал Fn — текст идёт в отладочное окно; работает с выключенным Wi-Fi | 2 д |
| 4 | Плашка | `OverlayPanel`, пилюля (стекло, перетаскивание к углам) и остров (форма выреза, экраны без выреза), состояния, честная волна, живой текст с фиксацией слов, звуки, «Уменьшить движение» | На встроенном экране с вырезом, на внешнем мониторе, поверх полноэкранного приложения; фокус не уходит | 2 д |
| 5 | Вывод и история | Вставка через буфер с восстановлением, раскладко-независимый ⌘V, защита полей пароля, три режима вывода, перетаскивание карточки, «Вставить последнее», авто-Enter, история в SwiftData | Матрица: Cursor, VS Code, iTerm2 + Claude Code, Terminal, Safari и Chrome (адресная строка), Telegram, Xcode. Буфер после вставки прежний | 1,5 д |
| 6 | Структура текста | Чистка и словарь, конвертация RUPunct в Core ML (`tools/`), абзацы по паузам, Qwen через MLX с лимитом времени, валидатор, откат по стадиям, «как сказал / оформлено» | Юнит-тесты валидатора (подмена слова отклоняется, удаление «эээ» проходит); задержка на наборе фраз | 2 д |
| 7 | Меню и настройки | Меню с карточкой последней диктовки, окно настроек со всеми разделами, окно истории с поиском и статистикой, запуск при входе | Каждая настройка меняет поведение без перезапуска | 1,5 д |
| 8 | Первый запуск | Восемь шагов онбординга по макетам, живые проверки разрешений, загрузка моделей с прогрессом, «попробуй», сворачивание в меню | Чистый пользователь macOS проходит от первого запуска до первой вставки | 2 д |
| 9 | Полировка | Иконка приложения, звуки, выгрузка LLM после простоя, энергопотребление в фоне, `implementation.md` | Приложение висит сутки в фоне без роста памяти | 1 д |

Итого около 14,5 рабочих дней.

Порядок выбран так, чтобы самое рискованное проверялось раньше: замеры (этап 2) решают, какие
модели качать. Ядро диктовки и плашка (этапы 3–4) — то, ради чего всё. Онбординг собирается
последним: он показывает то, что к этому моменту уже работает.

## Фразы для замеров

Читать естественно, в своём темпе. Каждая фраза — отдельная запись.

1. Поправь useEffect в компоненте Header, он дёргается при каждом рендере.
2. Задеплой ветку feature/auth на Vercel и скинь мне ссылку на превью.
3. Открой package.json и обнови Next.js до последней версии.
4. Добавь в Supabase таблицу users с полями id, email и created_at.
5. Почему TypeScript ругается на Promise void в этом хуке?
6. Сделай коммит с сообщением «fix: убрал лишний console.log».
7. Напиши тест на Jest для функции parseDate: пустая строка, ISO и timestamp.
8. Перенеси стили с Tailwind на CSS-модули, но только для страницы settings.
9. Запусти npm run dev и посмотри, что падает в консоли.
10. Слушай, короче, надо, чтобы при клике на кнопку Save данные уходили в API, а потом показывался тост.
11. Прочитай README и объясни, как устроен роутинг в этом проекте.
12. Во-первых, убери дублирование. Во-вторых, вынеси константы. В-третьих, добавь типы.
13. Подними Docker Compose с Postgres и Redis на стандартных портах.
14. В SwiftUI сделай кнопку со стилем glass и анимацией bouncy.
15. Проверь, почему GitHub Actions падает на шаге build.
16. Нужен эндпоинт POST /api/leads, который валидирует тело через Zod.
17. Переименуй переменную userData в currentUser во всём проекте.
18. Сделай так, чтобы в Telegram-боте команда /start отправляла приветствие.
19. Эээ, нет, подожди, не удаляй миграцию, лучше откати её.
20. Добавь в .env ключ OPENAI_API_KEY и прочитай его через process.env.
21. Оптимизируй запрос в Prisma, там N+1 на связи posts и comments.
22. Открой localhost:3000 в Chrome и проверь вёрстку на ширине 375 пикселей.
23. Используй React Query вместо useState для загрузки списка заказов.
24. Сделай ревью пулл-реквеста номер 42 и оставь комментарии по безопасности.
25. Свободный рассказ на 40–60 секунд: опиши любую фичу, которую хочешь сделать, с паузами и перечислениями — проверка абзацев и списков.
