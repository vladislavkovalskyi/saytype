name: saytype
options:
  bundleIdPrefix: dev.kovalskyi
  deploymentTarget:
    macOS: "26.0"
  createIntermediateGroups: true
  developmentLanguage: en

settings:
  base:
    SWIFT_VERSION: "6.0"
    DEVELOPMENT_TEAM: SHFR8S4R37
    CODE_SIGN_STYLE: Automatic
    CODE_SIGN_IDENTITY: "Apple Development"
    ENABLE_HARDENED_RUNTIME: YES
    MARKETING_VERSION: "0.3.0"
    CURRENT_PROJECT_VERSION: "1"

packages:
  SaytypeKit:
    path: Packages/SaytypeKit
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    from: 2.10.0

targets:
  Saytype:
    type: application
    platform: macOS
    sources:
      - path: App/Sources
      - path: App/Resources
        excludes:
          - "Fonts/*.txt"
      - path: App/Resources/Fonts/Onest-OFL.txt
        buildPhase: resources
      - path: App/Resources/Fonts/JetBrainsMono-OFL.txt
        buildPhase: resources
    dependencies:
      - package: SaytypeKit
        product: VMCore
      - package: SaytypeKit
        product: VMAudio
      - package: SaytypeKit
        product: VMTranscription
      - package: SaytypeKit
        product: VMSystem
      - package: SaytypeKit
        product: VMSmart
      - package: Sparkle
        product: Sparkle
    entitlements:
      path: App/Saytype.entitlements
      properties:
        com.apple.security.app-sandbox: false
        com.apple.security.device.audio-input: true
    info:
      path: App/Info.plist
      properties:
        CFBundleDisplayName: saytype
        CFBundleName: saytype
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        LSApplicationCategoryType: public.app-category.productivity
        LSUIElement: true
        # Translations live in App/Resources/InfoPlist.xcstrings.
        NSMicrophoneUsageDescription: saytype listens to the microphone while the record key is held. Audio is transcribed on this Mac and never leaves it.
        NSHumanReadableCopyright: © 2026 saytype
        # Sparkle 2. Generate the key pair with Sparkle's generate_keys and paste the public
        # key here; docs/RELEASING.md has the steps. The updater stays off while the
        # placeholder is in place.
        SUFeedURL: https://vladislavkovalskyi.github.io/saytype/appcast.xml
        SUPublicEDKey: tJXCduZaPnFP2Tm8wKQi/VX2lUrJ1axWe+solKFKUno=
        SUEnableAutomaticChecks: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.kovalskyi.saytype
        PRODUCT_NAME: saytype
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: NO
        COMBINE_HIDPI_IMAGES: YES
        # English source strings; Localizable.xcstrings and InfoPlist.xcstrings hold the translations.
        LOCALIZATION_PREFERS_STRING_CATALOGS: YES
        SWIFT_EMIT_LOC_STRINGS: YES
